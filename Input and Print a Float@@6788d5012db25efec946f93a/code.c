#include <stdio.h>
int main() {
    char word[100];
    scanf("%s", &word);
    printf("You entered: %s", word);
    return 0;
}
#include <stdio.h>
int main() {
    char word[100];
    scanf("%s", &word);
    printf("You entered: %s", word);
    return 0;
}